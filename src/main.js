/**
 * The entry. The app lives in src/app, one module per screen, and boot.js is
 * the one that runs: it wires the DOM, restores the session and paints the
 * first screen. This file exists so index.html has a single thing to load.
 */
import './app/boot.js';
